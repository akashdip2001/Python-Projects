```go
pip install opencv-python # Take screenShort every mili sec.
pip install pywin32 # Check the screen resolution
pip install numpy # Marge all SS using Array --> for video
pip install pyautogui
```

